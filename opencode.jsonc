{
    "$schema": "https://opencode.ai/config.json",
    "permission": {
        // -------------------------------------------------------------------------
        // Skills.

        "skill": {
            "test-run-whisper-mps-cli": "allow",
        },

        // -------------------------------------------------------------------------
        // Granular controls.

        "bash": {
            "whisper-mps *": "allow",
        },
    }
}
